# uniplac-dev-web-fastapi

# How to start the application

### With hot reload

On your terminal run:

```sh
uvicorn main:app --reload
```

### Without hot reload

On your terminal run:

```sh
uvicorn main
```
